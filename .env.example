# ibl.ai configuration — copy to .env.local (gitignored) and set your values.
# The four URLs below mirror the code defaults in lib/iblai/config.ts (hosted
# iblai.app), so the app works without them — they are listed here as the
# knobs to change when self-hosting. NEXT_PUBLIC_MAIN_TENANT_KEY is required;
# IBLAI_API_KEY (a secret — never commit it; the app checks it against the
# platform and renders nothing but an alert while it is missing, the
# placeholder below, rejected, or another platform's) unlocks server-side
# platform API calls via config.apiKey(), and doubles as a standard OpenAI
# api key at
# https://asgi.data.<domain>/api/ai-mentor/orgs/<platform-key>/v1 (chat completions
# + models) for the app's LLM features — no separate provider key needed.
# Distributed self-hosted mode: delete the NEXT_PUBLIC_API_BASE_URL line
# (and set your own NEXT_PUBLIC_PLATFORM_BASE_DOMAIN) so each service derives
# its own subdomain — editing only the domain keeps you on this single origin.
NEXT_PUBLIC_API_BASE_URL=https://api.iblai.app
NEXT_PUBLIC_AUTH_URL=https://login.iblai.app
NEXT_PUBLIC_BASE_WS_URL=wss://asgi.data.iblai.app
NEXT_PUBLIC_PLATFORM_BASE_DOMAIN=iblai.app
NEXT_PUBLIC_MAIN_TENANT_KEY=your-platform
IBLAI_API_KEY=your-token

# What the join page ("Join <name>") and the browser tab call this app. The
# platform's own name is never used for it; empty reads "this app".
NEXT_PUBLIC_APP_NAME=

# The app's public origin, used in the sign-up and Stripe return URLs. Empty
# (the default, and always on ibl.ai hosting) means the origin each request
# arrives on; set it behind a proxy that hides the host, or when the browser
# reaches the app by a name other than the one in the platform's allowed
# redirect origins.
IBLAI_APP_BASE_URL=

# The one agent this app fronts: last path segment of
# https://os.ibl.ai/platform/<platform-key>/<agent-uuid>. Never invent one.
NEXT_PUBLIC_DEFAULT_AGENT_ID=
NEXT_PUBLIC_SUPPORT_EMAIL=

# Paywall (server-only; see README "Paywall"). PAYWALL_APP_SLUG names this app
# on the platform: it tags the Stripe product (metadata.app, the platform's
# checkout rule) and keys the paywall choice in the platform's public metadata
# (apps.<slug>), which the in-app setup at /setup writes. PAYWALL_PRICE_IDS is
# an optional comma-separated override that wins over that choice.
PAYWALL_APP_SLUG=vibe-agent
PAYWALL_PRICE_IDS=

# Mobile (Tauri iOS/Android) SSO return scheme. Desktop and web ignore it.
NEXT_PUBLIC_TAURI_CUSTOM_SCHEME=vibe-agent

# Show the About tab and /about (off by default).
NEXT_PUBLIC_SHOW_ABOUT=false
